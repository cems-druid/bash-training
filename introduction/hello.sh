#!/bin/bash

#In order to execute or run a bash script with the bash shell interpreter, first line should be like above. 
#Select bash interpreter and use bash [x.sh] to execute. So for this file bash hello.sh
echo "Hello world with bash"

#You can assign variables but be careful with spaces around it. There should be none.
avariable="My first variable in bash."
dowhat="variable string-"
echo ${avariable}
echo "Let's put into ${dowhat} the string"